# Patterns rsync sẽ KHÔNG đẩy lên server.
# Dùng cùng deploy.sh:  rsync --exclude-from=.rsync-exclude ...

# Python
.venv/
venv/
env/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# Git (server có thể dùng repo riêng, tránh ghi đè)
.git/
.gitignore

# Model weights (nặng — copy thủ công 1 lần)
models/*.pt
models/*.onnx
models/*.engine
models/paddleocr/

# Logs & runtime artifacts
*.log
logs/
runs/
output/
*.mp4
*.avi

# Local-only
.env
.env.*
deploy.sh.local
